- hosts: localhost
  tasks:
    - name: test something
      assert:
        that:
          - (1 + 1) == 2
          - '"apples" in "oranges"'
          - true
        msg: something is wrong with something

    - name: show that playbook continues after assertion failure
      debug:
        msg: "playbook execution continues"
